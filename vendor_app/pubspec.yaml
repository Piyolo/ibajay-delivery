name: ibajay_eats_vendor
description: Vendor app for Ibajay Eats — manage your store, menu, orders and deliveries.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # State management
  provider: ^6.1.2

  # Networking (wired up once the FastAPI backend is connected)
  http: ^1.2.1
  http_parser: ^4.0.2
  web_socket_channel: ^2.4.0

  # Formatting / utils
  intl: ^0.19.0
  fl_chart: ^0.68.0

  # Media & maps
  image_picker: ^1.1.2
  # OpenStreetMap-based maps (no API key required) for store-address picking
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # Live GPS pings while a delivery is in progress
  geolocator: ^13.0.4

  # Persistent storage for picked logo/banner images
  path_provider: ^2.1.4

  # Local persistence for the auth session token
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  image_path: "assets/images/ibajay_eats_logo.png"
  android: "ic_launcher"

flutter:
  uses-material-design: true
  assets:
    - assets/images/